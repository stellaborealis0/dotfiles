<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.gerardo.kilo.env-loader</string>
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>-c</string>
        <string>set -e
if [ -f "$HOME/.env.kilo.secrets" ]; then
    source "$HOME/.env.kilo.secrets"
fi
if [ -f "$HOME/.env.kilo" ]; then
    source "$HOME/.env.kilo"
fi
launchctl setenv REQUESTY_API_KEY "${REQUESTY_API_KEY:-}" 2>/dev/null || true
launchctl setenv NVIDIA_API_KEY "${NVIDIA_API_KEY:-}" 2>/dev/null || true
launchctl setenv DASHSCOPE_API_KEY "${DASHSCOPE_API_KEY:-}" 2>/dev/null || true
launchctl setenv GROQ_API_KEY "${GROQ_API_KEY:-}" 2>/dev/null || true
launchctl setenv OPENROUTER_API_KEY "${OPENROUTER_API_KEY:-}" 2>/dev/null || true
launchctl setenv KILO_MACHINE_NAME "${KILO_MACHINE_NAME:-}" 2>/dev/null || true
launchctl setenv KILO_DOOM_LOOP_PROTECTION "${KILO_DOOM_LOOP_PROTECTION:-true}" 2>/dev/null || true
launchctl setenv KILO_MAX_PARALLEL_AGENTS "${KILO_MAX_PARALLEL_AGENTS:-2}" 2>/dev/null || true
launchctl setenv KILO_AUTO_APPROVE_TIMEOUT "${KILO_AUTO_APPROVE_TIMEOUT:-10000}" 2>/dev/null || true
</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <false/>
    <key>StandardOutPath</key>
    <string>/tmp/kilo_env_loader_stdout.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/kilo_env_loader_stderr.log</string>
</dict>
</plist>
